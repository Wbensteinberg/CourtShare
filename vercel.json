{
  "buildCommand": "npm run build",
  "installCommand": "npm install && npm install --no-save --platform=linuxmusl --arch=x64 lightningcss @tailwindcss/oxide",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/notifications/send-scheduled",
      "schedule": "0 * * * *"
    }
  ]
}
